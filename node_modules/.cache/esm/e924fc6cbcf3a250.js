let minimist,help,version,testSpawn,setup;_bcf‍.x([["cli",()=>cli]]);_bcf‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_bcf‍.w("./help.js",[["help",["help"],function(v){help=v}]]);_bcf‍.w("./version.js",[["version",["version"],function(v){version=v}]]);_bcf‍.w("./test-spawn.js",[["testSpawn",["testSpawn"],function(v){testSpawn=v}]]);_bcf‍.w("./setup.js",[["setup",["setup"],function(v){setup=v}]]);





       async function cli(argsArray) {
    const args = minimist(argsArray.slice(2));

    let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  if(args.list || args.l) {
    cmd = 'testSpawn'
  }

  if (args.setup || args.s){
    cmd = 'setup'
  }

 

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'testSpawn':
      testSpawn(args);
      break;

    case 'setup':
      setup(args);
      break;
    
    case 'add':
      add(args);
      break;

    
  


  }


}
