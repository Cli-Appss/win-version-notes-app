let minimist,help,version,testSpawn,setup,add;_e30‍.x([["cli",()=>cli]]);_e30‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_e30‍.w("./help.js",[["help",["help"],function(v){help=v}]]);_e30‍.w("./version.js",[["version",["version"],function(v){version=v}]]);_e30‍.w("./test-spawn.js",[["testSpawn",["testSpawn"],function(v){testSpawn=v}]]);_e30‍.w("./setup.js",[["setup",["setup"],function(v){setup=v}]]);_e30‍.w("./add.js",[["add",["add"],function(v){add=v}]]);






       async function cli(argsArray) {
    const args = minimist(argsArray.slice(2));

    let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  if(args.list || args.l) {
    cmd = 'testSpawn'
  }

  if (args.setup || args.s){
    cmd = 'setup'
  }
  
  if(args.add || args.a){
    cmd = 'add'
  }

 

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'testSpawn':
      testSpawn(args);
      break;

    case 'setup':
      setup(args);
      break;
    
    case 'add':
      add(args);
      break;

    
  


  }


}
