let minimist,help,version,testSpawn;_bc8‍.x([["cli",()=>cli]]);_bc8‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_bc8‍.w("./help.js",[["help",["help"],function(v){help=v}]]);_bc8‍.w("./version.js",[["version",["version"],function(v){version=v}]]);_bc8‍.w("./test-spawn.js",[["testSpawn",["testSpawn"],function(v){testSpawn=v}]]);




       async function cli(argsArray) {
    const args = minimist(argsArray.slice(2));

    let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  if(args.list || args.l) {
    cmd = 'testSpawn'
  }

 

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'testSpawn':
      testSpawn(args);
      break;

    
  


  }


}